## Packages
react-hook-form | Form state management
@hookform/resolvers | Zod integration for forms
react-to-print | Printing resume component to PDF
framer-motion | Smooth animations and page transitions
date-fns | Date formatting for resume entries
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
Authentication is handled via Replit Auth (use-auth.ts hook already exists)
Resume PDF export uses browser print functionality via react-to-print targeting the Preview component
Backend API routes follow REST patterns defined in shared/routes.ts
